server:
  port: 8888
  servlet:
    context-path: /
  tomcat:
    uri-encoding: UTF-8
    max-threads: 1000
    min-spare-threads: 30

app:
  file-dest: "F:/workspace/postmanToWord/uploads"
  file-path-route: "/uploads/" # 上传的访问地址
  file-visit-host: "http://localhost:8888" # 上传的文件地址访问主机名
